// API configuration — used by Next.js API routes only (server-side)
// Client-side pages call /api/... local routes which proxy to this.
export const API_BASE = process.env.API_URL || "https://api.fastdol.com";
